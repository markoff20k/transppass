# Modo mock: front sozinho, sem backend e sem banco.
# Carregado pelo Vite com `--mode mock` (npm run dev:mock).
#
# VITE_API_URL vazio faz o cliente usar caminhos relativos (/api), que o
# service worker do MSW intercepta antes de qualquer ida a rede.
VITE_API_URL=
VITE_MOCK=true
